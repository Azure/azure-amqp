#include "connection.h"

CONNECTION_HANDLE connection_create(IO_HANDLE io)
{
	(void)io;
	return NULL;
}

void connection_destroy(CONNECTION_HANDLE handle)
{
	(void)handle;
}

void connection_dowork(CONNECTION_HANDLE handle)
{
	(void)handle;
}
